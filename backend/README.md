# Backend

This module is designed for the FastAPI + LangGraph system described in `plan.md`.

## Structure
- `app/api/`: FastAPI routes and streaming handlers
- `app/agents/`: LangGraph definitions, tools, and agent nodes
- `app/core/`: Configuration, Pydantic state models, and common utilities
